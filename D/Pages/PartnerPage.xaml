<Page x:Class="D.Pages.PartnerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:D.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="Страница партнеров">

    <Grid>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <Button Content="Назад" Margin="5" Click="Back_Click"/>
        </StackPanel>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
            <DataGrid x:Name="Partner" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Тип партнера" Binding="{Binding type}" Width="100"/>
                    <DataGridTextColumn Header="Наименование партнера" Binding="{Binding name}" Width="100"/>
                    <DataGridTextColumn Header="Фамилия Директора" Binding="{Binding second_name}" Width="300"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding first_name}" Width="100"/>
                    <DataGridTextColumn Header="Отчество" Binding="{Binding middle_name}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Перейти к пользователям" Click="Button_Click"></Button>
        </StackPanel>
    </Grid>
</Page>
